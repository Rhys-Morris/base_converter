{
  "name": "base_converter",
  "version": "1.0.0",
  "description": "Application for converting numerals between different bases",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass ./sass/main.scss ./main.css --watch"
  },
  "author": "Rhys Morris",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.32.4"
  }
}
